# single-r9700 profile index

Profiles for one R9700 card in this branch:

- `muse-glimmer-30b/v1/single-r9700` (experimental)

`qwen38-flash-next` currently has **no single-card profile**; Qwen3.8 Flash Next
is not validated as a single-GPU setup in this branch.
